{
  "name": "rheumatology-immunology-frontier",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "update": "node scripts/update-feed.mjs --days 1 --limit 100 --output data/items.json",
    "check": "node scripts/verify-site.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
